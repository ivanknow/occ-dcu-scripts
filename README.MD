#### Custom OCC DCU scripts that simplify the commands so you don't have to type repeated values everytime.

### REQUIREMENTS

1. Install Node.js
2. Install Git Bash on Windows to run .sh files 

### CONFIGURATION
	
1. Open git bash and run install.sh
